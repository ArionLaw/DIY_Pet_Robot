
# placeholder states
receivingSerial = True
SerialMessageMakesSense = True

while (receivingSerial == True):
    if (SerialMessageMakesSense == True):
    # read Arduino Serial
    # parse?
    # compute or update current FK
    # read keyboard input
    # compute desired IK
    # write desired joint values to Arduino Serial

    else():
        # error and exception handling
